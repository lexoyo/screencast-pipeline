# Proper nouns whisper gets wrong. One per line:
#
#     Nom Canonique = mauvaise transcription, autre mauvaise transcription
#
# Used twice: the canonical spellings prime whisper as its initial prompt, and the whole
# table corrects what still slipped through afterwards.
#
# Case, accents and spacing are handled automatically — "hugging face" finds "Hugging
# Face" on its own. Aliases are for PHONETIC mistakes, which cannot be guessed: "Cloudcode"
# and "Claude Code" are simply different words. Add one the day a name comes out mangled.
#
# ORDER MATTERS. The prompt is capped, and the tail is what gets dropped — so the names of
# what the channel is currently talking about go FIRST, and the general vocabulary last.

# The subject of the current videos.
Goose = GUZ, Gouz, Gouze, Gousse, Gooz
Claude Code = Cloudcode, Cloud code, Cloud Cowork, Clode code, Claudecode
Codex = Codexe, Codex e
Ollama = Olama, O Lama, Aulama
LM Studio = LMStudio
llamafile = lama file, llama file, LamaFile
Jan = Djann, Jann, Djan
OpenRouter = Open Router
Qwen = Couen, Qwenn
Anthropic = Antropique
Hugging Face = huggingface

# The channel's own tools and projects.
Silex = Cylex, Silexe
PeerTube = Peer Tube, Pertube
ffmpeg = FFM peg, effe em peg
whisper.cpp = whisper CPP, whisper c++
Shotcut = Shot cut, Chotcut
OBS = O B S, obès
WordPress = Word Press

# General vocabulary. Long tail: may fall outside the prompt, still corrected afterwards.
GitHub = Git Hub
GitLab = Git Lab
Mastodon
Fedora
Linux
open source = opensource
LLM = L L M, èl èl èm
MCP = M C P, emcépé, mcpé
GPU
VRAM = V RAM
MKV = M K V, em ka vé
Flathub = Flat hub
ordi = ARDI, hardi
prompt = prompte
prompter = prompté
